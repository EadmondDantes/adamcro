export interface AnimationProps {
  initial?: object;
  animate?: object;
  transition?: object;
  className?: string;
}

export interface MetaProps {
  title: string;
  description: string;
}

export interface IconProps {
  className?: string;
  'aria-hidden'?: boolean;
}